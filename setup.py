import os
import sys,getopt
import re
from distutils.extension import Extension
from setuptools import setup, find_packages
setup(name="score_card", version="0.1.0",
    packages=find_packages(),
    author="weiwei.lin", author_email=None,
    description="score card model")

# EOF
